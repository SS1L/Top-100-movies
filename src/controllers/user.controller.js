const login = (req, res) => {
  console.log('all work');
};

const registration = (req, res) => {
  console.log('all work');
};

const logout = (req, res) => {
  console.log('logout');
};

module.exports = {
  login,
  registration,
  logout,
};
